<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/caqui</item>
        <item name="colorPrimaryDark">@color/caquiOscuro</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme2" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/caqui</item>
        <item name="colorPrimaryDark">@color/caquiOscuro</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="editTextColor">@color/morado</item>
    </style>

    <style name="AppThemeNoActionBar" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/caqui</item>
        <item name="colorPrimaryDark">@color/caquiOscuro</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="MyDarkToolbarStyle" parent="ThemeOverlay.AppCompat.Light">
        <item name="colorPrimary">@color/caqui</item>
        <item name="colorPrimaryDark">@color/caquiOscuro</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColor">@color/morado</item>
        <item name="android:textColorPrimary">@color/morado</item>
        <item name="colorControlNormal">@color/morado</item>

    </style>


</resources>
